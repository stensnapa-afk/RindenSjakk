# RindenSjakk — Python dependencies (installeres av install.bat i et venv)
python-chess==1.11.2          # regelmotor: FEN-tidslinje -> lovlig PGN-tre
opencv-python-headless>=4.10  # brett-lokalisering + brikkegjenkjenning
numpy>=1.26                   # bilde/frame-matte
yt-dlp>=2026.8                # last ned YouTube-video (krever Node.js som JS-runtime)

# Valgfri, senere fase (ikke nødvendig for å kjøre):
#   faster-whisper            # Fase 3: lokal tale-til-tekst som kryssjekk (ingen sky-GPU)
