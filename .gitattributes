# Mac/Linux shell launchers MUST stay LF, or bash fails with "bad interpreter".
*.command text eol=lf
*.sh      text eol=lf
# Windows batch prefers CRLF.
*.bat     text eol=crlf
# Everything else: normalize to LF in the repo.
* text=auto
